---
description: ThinkPHP 8 命名与目录约定（官方开发规范）
globs: app/**/*.php,config/**/*.php,route/**/*.php
alwaysApply: false
---

# ThinkPHP 8 命名

遵循 [官方开发规范](https://doc.thinkphp.cn/v8_0/development_specifications.html)；本地对照 [`docs/thinkphp/v8/DevelopmentSpecification.md`](docs/thinkphp/v8/DevelopmentSpecification.md)。目录即命名空间（PSR-4）。

- 目录：`小写+下划线`；类文件大驼峰，且与类名一致
- 类：`User`、`UserType`；控制器/模型**不加** `Controller`/`Model` 后缀
- 方法、属性：小驼峰 `getUserName`、`tableName`
- `app/common.php` 助手函数：`小写+下划线` `get_client_ip()`
- 常量：`APP_PATH`；配置键：`url_route_on`；环境变量：`APP_DEBUG`
- 表和字段：`think_user`、`user_name`；禁止驼峰、中文、以下划线开头

```php
// ❌
class user_controller {}
function GetUserName() {}

// ✅
namespace app\controller;
class User
{
    public function getUserName() {}
}
```
