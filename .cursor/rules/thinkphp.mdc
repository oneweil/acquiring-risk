---
description: ThinkPHP 8 命名约定 + 本地 docs/thinkphp 检索用法
globs: app/**/*.php,config/**/*.php,route/**/*.php,database/**/*
alwaysApply: false
---

# ThinkPHP 8

权威在线：[官方手册](https://doc.thinkphp.cn/v8_0/preface.html)；本地镜像：[`docs/thinkphp/`](docs/thinkphp/README.md)。写框架代码前先检索，勿整本粘贴进对话。

## 命名（官方开发规范）

对照 [`docs/thinkphp/v8/DevelopmentSpecification.md`](docs/thinkphp/v8/DevelopmentSpecification.md)。目录即命名空间（PSR-4）。

- 目录：`小写+下划线`；类文件大驼峰，且与类名一致
- 类：`User`、`UserType`；控制器/模型**不加** `Controller`/`Model` 后缀
- 方法、属性：小驼峰 `getUserName`、`tableName`
- `app/common.php` 助手：`小写+下划线` `get_client_ip()`
- 常量：`APP_PATH`；配置键：`url_route_on`；环境变量：`APP_DEBUG`
- 表和字段：`think_user`、`user_name`；禁止驼峰、中文、以下划线开头

## 写代码前

1. **先检索** `docs/thinkphp/v8/`（可用 `@docs/thinkphp`）
2. 按手册 API 写，**不要**用 Laravel 习惯硬套（如假定存在官方 JsonResource）
3. 本地与官网冲突时以 **官网** 为准

| 需求 | 先读 |
|------|------|
| 命名 / 目录 | `DevelopmentSpecification.md` |
| 列表分页 | `QueryBuilderPagedQuery.md` → `paginate()` |
| 模型输出 | `ModelModelOutput.md` |
| 迁移 | `DatabaseMigrationTool.md` + 规则 `db-migration-seed` |
| JSON 响应 | 后台 `AdminBase::success`/`fail`；API `ApiBase` 同类 |
| 资源路由 ≠ API Resource | `ResourceRouting.md` 是 REST 路由 |

框架怎么用 → `docs/thinkphp/`；风控业务 → `docs/01–07` 与 `risk-architecture`。
