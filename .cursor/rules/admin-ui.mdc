---
description: 后台列表 CRUD + 前端优先 Tabler 组件（Validate/Repo/Resource + AJAX）
globs: view/**/*,public/static/js/**/*,public/static/css/**/*,app/controller/admin/**/*,app/validate/**/*,app/repository/**/*,app/resource/**/*,route/app.php
alwaysApply: false
---

# 后台 Admin 列表

参考实现：黑名单 + 处置策略（`Blacklist` / `Disposition` 全套）。  
脚手架：`.cursor/skills/admin-list-crud/SKILL.md`。

## 前端 UI（Tabler）

- **尽量只用 Tabler UI 组件与工具类**（`/static/tabler/`：card、btn、form、modal、alert、table、nav、badge、dropdown 等；图标用 `ti ti-*`）。对照 [Tabler 文档](https://docs.tabler.io)
- **禁止**再引入 Bootstrap / Ant Design / Element 等第二套 UI，或自造与 Tabler 平行的组件皮肤
- **例外**：风控总览仪表盘可用 ApexCharts（`/static/apexcharts/` + `dashboard.js`）做图表；仅限图表，页面壳与控件仍用 Tabler；勿扩到其它模块或另引 Chart.js/ECharts 等
- 项目增量样式只放 `public/static/css/custom.css`，仅做布局微调/主题色，不另起组件体系
- 交互优先复用已有封装：`public/static/js/admin/ui.js`（确认框、Alert）、`list-page.js`

## 分层

| 层 | 做 | 不做 |
|---|---|---|
| Validate | `list`/`save` 场景；枚举与 `page`/`pageSize` 校验；`toListFilters` / `toListPageSize` / `toSaveData` | 写库 |
| Repository | `isset($filters[k])` 后直接 `where`；`paginate` | 再 `in_array` 校验枚举；拼 `*_label` |
| Resource | 对外字段与 `*_label`；列表 `XxxResource::paginate` | 写库 |
| Service | 跨 Repo 组装、批量写回后再拼 list payload 等编排 | HTTP / Validate |
| Controller | 校验 → 调 Service/Repo → Resource；`success`/`fail` | **禁止新增 private/protected 业务方法**（组装逻辑放 Service） |

- 控制器**只保留**公开 action（`index`/`list`/`save`…）；基类 `AdminBase` 的 protected 辅助除外
- `toListFilters`：**只放入非空条件**（勿返回一堆空字符串）
- 列表 `page` 已校验则用 `(int) ($params['page'] ?? 1)`，勿再 `max(1, …)` 重复兜底业务校验

## 接口

- `GET /admin/{module}/list`：query 含筛选 + `page` + **`pageSize`**
- `pageSize` 允许值：**`10` / `20` / `50`**（无 `100`）；缺省用 `config('paginate.list_rows')`
- 列表 `data`：ThinkPHP 分页形状（`data`/`total`/`per_page`/`current_page`/`last_page`）
- 保存：`POST .../save`；校验失败 `fail(..., 422, null, 422)`
- 路由：**更长路径写在短路径前**

## 列表页前端

- `ListPage.escapeHtml` / `ListPage.renderPageSizeDropdown`（`list-page.js`）
- 页脚：左侧每页条数下拉（`20 条/页`）；右侧「共 N 条记录」+ 分页（左右 chevron）
- 分页 UI 用 `ListPage.renderPaginationHtml` / `renderPageSizeDropdown`
- 请求与 URL query 同步：`page`、`pageSize`、筛选字段
- option **value=英文**；展示用 `*_label`
- Modal 同页新增/编辑；无硬删时用状态失效
- **配置页（非列表）首屏数据**：经 `renderList` / 视图变量服务端注入，**不要**再单独写 `GET .../config` 异步拉取；仅列表分页数据用 AJAX `GET .../list`；保存等写操作仍用 `POST .../save`

## 其它

- 枚举入库英文；`AdminBase::renderList` 渲染已实现页
- 处置策略等模块**无关键字搜索**时不要硬加 keyword；黑名单可保留 keyword
- 禁止跨库 SQL JOIN（见 `risk-architecture`）
